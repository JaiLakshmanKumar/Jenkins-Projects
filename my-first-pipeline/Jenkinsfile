pipeline {
  agent {
    docker { image 'maven:3.9.6-eclipse-temurin-17' }
  }
  stages {
    stage('Test') {
      steps {
        // Use 'mvn', not 'maven'
        sh 'mvn --version'
      }
    }
  }
}